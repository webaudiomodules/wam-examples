{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "build": "lerna run build --stream",
    "start": "lerna run start --stream"
  },
  "workspaces": [
    "packages/*"
  ]
}
